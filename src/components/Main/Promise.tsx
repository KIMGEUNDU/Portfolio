import styled from 'styled-components';

function Promise() {
  return <Wrapper>Promise</Wrapper>;
}

const Wrapper = styled.div`
  width: 100vw;
  height: 100vh;
  background-color: pink;
`;

export default Promise;
